[pytest]
testpaths = tests
asyncio_mode = auto
markers =
    unit: fast unit tests (no network)
    integration: integration tests (real filesystem, no LLM)
    e2e: E2E smoke tests (real Qwen + real AIO-Sandbox, ~5 min)
    e2e_full: E2E full journey tests (real Qwen + real sandbox, 15-40 min)
